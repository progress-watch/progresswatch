<p><%= t('the_server_is_open_source_under_the_agpl') %></p>
<h2><%= t('one_file') %></h2>
<p><%= t('docker_compose_yml_is_the_whole_install_html', file: tag.code('docker-compose.yml'), docker: link_to(t('the_docker_page'), docs_section_path(section: 'docker'))) %></p>
<%= render 'shared/command', body: "openssl rand -hex 64\ndocker compose up -d" %>
<p><%= t('that_is_the_whole_setup_the_app_on_localhost_html', url: tag.code('http://localhost:7979')) %></p>
<h2><%= t('configuring_it') %></h2>
<p><%= t('the_file_is_yours_from_the_moment_you_save_it_html', environment: tag.code('environment:')) %></p>
<%= render 'shared/command', body: "    environment:\n      - SECRET_KEY_BASE=8f3c…\n      - DATABASE_URL=postgresql://progresswatch:secret@db.internal/progresswatch\n      - VAPID_PUBLIC_KEY=BJ1w…\n      - VAPID_PRIVATE_KEY=xK2p…" %>
<p><%= t('two_services_run_the_same_image_and_both_boot_the_whole_application_html', both: tag.strong(t('both'))) %></p>
<p><%= t('environment_variables_is_the_full_list_html', link: link_to(t('environment_variables'), docs_section_path(section: 'environment-variables')), ports: tag.code('7979:3000')) %></p>
<%= render 'shared/command', body: %(curl http://localhost:7979/up\n\n{"status":"ok","database":true,"redis":true,"worker":true}) %>
<h2><%= t('what_is_running') %></h2>
<p><%= t('three_processes_and_a_file_the_app_answers_requests') %></p>
<h2><%= t('why_7979') %></h2>
<p><%= t('not_3000_this_is_a_service_you_leave_running_for_weeks_html', run: tag.code('docker run')) %></p>
<p><%= t('inside_the_container_it_is_still_3000_html', ports: tag.code('7979:3000')) %></p>
<h2><%= t('losing_the_volume_is_recoverable') %></h2>
<p><%= t('spaces_and_task_structure_are_on_it_and_nothing_else') %></p>
<h2><%= t('sqlite_or_postgresql') %></h2>
<p><%= t('both_from_the_same_image_and_the_same_migrations_html', database_url: tag.code('DATABASE_URL'), postgres: tag.code('postgresql://…')) %></p>
<h2><%= t('behind_a_proxy') %></h2>
<p><%= t('set_force_ssl_true_once_something_in_front_terminates_tls_html', force_ssl: tag.code('FORCE_SSL=true')) %></p>
<h2><%= t('notifications') %></h2>
<p><%= t('set_a_vapid_key_pair_and_a_notify_me_button_appears_html', notify: t('notify_me'), link: link_to(t('notifications_lowercase'), docs_section_path(section: 'notifications'))) %></p>
<h2><%= t('sweeping_empty_spaces') %></h2>
<p><%= t('opening_the_site_creates_a_space_for_you_html', ever: tag.strong(t('ever'))) %></p>
<p><%= t('nothing_runs_it_for_you_schedule_it_however_you_already_schedule_things') %></p>
<%= render 'shared/command', body: 'bin/rails sweep_empty_spaces' %>
<%= render 'shared/command', body: "# #{t('crontab_daily_at_0417')}\n17 4 * * * cd /path/to/progresswatch && docker compose exec -T progresswatch bin/rails sweep_empty_spaces" %>
<p><%= t('because_nothing_is_on_a_timer_30_days_is_a_floor') %></p>
<h2><%= t('your_instance_is_not_indexable') %></h2>
<p><%= t('every_page_of_a_self_hosted_install_is_noindex_html', noindex: tag.code('noindex'), robots: tag.code('robots.txt'), sitemap: tag.code('/sitemap.xml')) %></p>
