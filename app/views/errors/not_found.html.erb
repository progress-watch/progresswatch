<div class="mx-auto max-w-md text-center">
  <h1 class="text-2xl font-semibold tracking-tight">
    <%= local_assigns[:heading] || 'Not found' %>
  </h1>
  <p class="mt-2 text-sm text-zinc-600 dark:text-zinc-400">
    <%= local_assigns[:explanation] || 'There is nothing at this address.' %>
  </p>
  <%= link_to 'Back to the start', root_path, class: 'btn-primary mt-6' %>
</div>
